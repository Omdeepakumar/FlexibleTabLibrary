package com.flexibletab.model;

/**
 * Enum defining different tab visual styles.
 */
public enum TabType {
    TEXT_ONLY,
    ICON_ONLY,
    IMAGE_ONLY,
    ICON_TEXT,
    IMAGE_TEXT,
    INDICATOR_ONLY,
    ROUNDED_PILL,
    UNDERLINE,
    MATERIAL,
    INSTAGRAM,
    TIKTOK
}
